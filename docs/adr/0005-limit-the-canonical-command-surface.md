# Limit the canonical command surface to artifact locations

The canonical reproduction commands expose model, data, output, and device locations but keep the paper's algorithm and inference parameters fixed in version-controlled configuration. This deliberately gives up a flexible experiment interface so that successful execution has an unambiguous relationship to the reported run.
