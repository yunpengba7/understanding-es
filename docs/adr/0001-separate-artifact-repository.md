# Keep the reproduction package separate from the manuscript repository

The public reproduction package lives in its own Git repository rather than inside the Overleaf-backed manuscript repository. This avoids nested-repository and synchronization ambiguity while allowing code, dependencies, data contracts, and release checks to evolve independently from the paper sources.
