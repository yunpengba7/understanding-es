# Keep the anonymous artifact separate from the manuscript repository

The anonymous code artifact lives in its own Git repository rather than inside the Overleaf-backed manuscript repository. This avoids nested-repository and synchronization ambiguity while allowing the artifact to have a clean, independently audited publication history.
