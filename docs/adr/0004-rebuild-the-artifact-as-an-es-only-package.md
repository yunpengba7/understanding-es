# Rebuild the artifact as an ES-only package

The artifact will preserve the executed ES behavior through focused modules and regression tests instead of copying the research repository's generic registries and cross-method inheritance. This requires careful equivalence testing, but it produces a reviewable implementation without unrelated training methods.
